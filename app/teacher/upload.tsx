 "use client";
import {useState} from "react";
export default function UploadLesson(){const [f,setF]=useState({title:"Water Cycle",subject:"Science",className:"Class 5",sourceText:"Water evaporates because of heat from the sun. Water vapour rises, cools and condenses into droplets that form clouds. Precipitation returns water to Earth."});const [msg,setMsg]=useState("");async function save(e:any){e.preventDefault();const r=await fetch("/api/lessons",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const d=await r.json();setMsg(r.ok?"Lesson saved. AI quiz can be generated from it.":d.error)}return <div className="card"><h2>➕ Upload lesson</h2><p className="muted">Paste lesson text now; production can add PDF/DOCX extraction behind the same API.</p><form onSubmit={save}><input className="input" placeholder="Title" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><input className="input" placeholder="Subject" value={f.subject} onChange={e=>setF({...f,subject:e.target.value})}/><textarea className="textarea" value={f.sourceText} onChange={e=>setF({...f,sourceText:e.target.value})}/><button className="btn green">Save & publish lesson</button>{msg&&<p className="notice" style={{marginTop:12}}>{msg}</p>}</form></div>}