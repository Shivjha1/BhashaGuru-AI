 "use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Register(){const [f,setF]=useState({name:"",email:"",password:"",role:"STUDENT",language:"Telugu",className:"Class 5",childEmail:""});const [msg,setMsg]=useState("");const r=useRouter();async function submit(e:any){e.preventDefault();setMsg("");try{const res=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const d=await res.json();if(!res.ok){setMsg(d.error||"Registration failed.");return}r.push("/login")}catch{setMsg("The server could not be reached. Please try again.")}}return <main className="wrap" style={{maxWidth:600}}><div className="card"><h1>Create BhashaGuru account</h1><form onSubmit={submit}>{[["name","Full name"],["email","Email"],["password","Password (6+ characters)"]].map(([k,l])=><div key={k}><label>{l}</label><input className="input" type={k==="password"?"password":"text"} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/></div>)}<label>Role</label><select className="select" value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="STUDENT">Student</option><option value="TEACHER">Teacher</option><option value="PARENT">Parent</option></select>{f.role==="PARENT"&&<><label htmlFor="childEmail">Student email</label><input id="childEmail" className="input" type="email" value={f.childEmail} onChange={e=>setF({...f,childEmail:e.target.value})} placeholder="Optional: link an existing student"/><p className="muted">Enter the student account email to show their details after parent login.</p></>}<label>Mother tongue</label><select className="select" value={f.language} onChange={e=>setF({...f,language:e.target.value})}><option>English</option><option>Hindi</option><option>Telugu</option><option>Tamil</option><option>Kannada</option><option>Marathi</option><option>Bengali</option></select>{msg&&<p className="notice danger">{msg}</p>}<button className="btn">Create account</button></form></div></main>}